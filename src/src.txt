Пути указываем абсолютные, мы переопределили конфигурацию (см. config-overrides.js), которая потом заменить /src на правильный путь от корневого каталога сервера
Пример:

import App from '/src/components/app/app';
